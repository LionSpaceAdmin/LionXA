# Copy this file to `.env` and fill in values

# Google Gemini API
GEMINI_API_KEY=

# X (Twitter) list to monitor (optional)
# Example: https://x.com/i/lists/123...
TWITTER_LIST_URL=

# Agent runtime behavior
# Set DRY_RUN=1 to disable real Gemini calls and tweet posting
DRY_RUN=1

# Browser configuration
# Set to 1 for interactive (visible) browser, 0/headless for CI
INTERACTIVE=1
HEADLESS_BROWSER=false
# Optional: persist user data between runs (absolute path recommended)
# BROWSER_USER_DATA_DIR=

# Prefer stable Chrome on macOS for best compatibility
# Options: chrome | chromium | msedge | chrome-beta | chrome-canary
BROWSER_CHANNEL=chrome
# Or specify an absolute executable path to override the channel
# BROWSER_EXECUTABLE_PATH=/Applications/Google Chrome.app/Contents/MacOS/Google Chrome

# Deep inspection / communication
# Open DevTools when launching (headful only)
# BROWSER_DEVTOOLS=true
# Expose a CDP remote debugging port for external tools
# BROWSER_DEBUG_PORT=9222
# Attach to an existing Chromium/Chrome via CDP (if port above is open)
# BROWSER_ATTACH=1
# Optional explicit CDP URL (defaults to http://127.0.0.1:$BROWSER_DEBUG_PORT)
# BROWSER_CDP_URL=http://127.0.0.1:9222
# Slow down actions (ms) for demos or debugging
# BROWSER_SLOWMO=0
# Extra Chromium args (comma-separated)
# BROWSER_ARGS=--disable-notifications,--disable-features=Translate

# Optional: start URL override for debugging
# START_URL=

# Dashboard client config (UI -> Socket.IO)
# Uncomment to override default behavior in the UI socket client
# NEXT_PUBLIC_DASHBOARD_URL=http://localhost:3001
# NEXT_PUBLIC_DASHBOARD_PORT=3001
# NEXT_PUBLIC_DASHBOARD_PATH=/socket.io

# Ports
# PORT_UI=3000
# DASHBOARD_PORT=3001

# Unified browser profile directory (for local dev)
# PROFILE_DIR=src/profiles/agent_profile/Default




# Avoid storing any real login credentials in the repo
# Xemail=you@example.com
# xUseu=username
# Xpass=super-secret
