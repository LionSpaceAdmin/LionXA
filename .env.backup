# Local backup of safe defaults for LionXA (no real secrets)

DRY_RUN=1
INTERACTIVE=1
HEADLESS_BROWSER=false
BROWSER_CHANNEL=chrome
GEMINI_API_KEY=
STEEL_API_URL=http://localhost:3001
USE_STEEL_BROWSER=false

# Add project-specific overrides locally only. Never commit production secrets.
