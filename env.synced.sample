# LionXA Production Environment Mapping (sample — no secrets)

# Public endpoints (filled after terraform apply)
# LB_URL=https://your-lb-domain
# RUN_SERVICE_URL=https://xagent-service-xxxx-uc.a.run.app

# UI ↔ Dashboard connectivity (same-origin via edge proxy)
# NEXT_PUBLIC_DASHBOARD_PATH=/socket.io

# Runtime ports (internal only; edge proxy exposes $PORT)
# UI_PORT=3002
# DASHBOARD_PORT=3011
# PORT=8080

# Agent/runtime configuration (set via Cloud Run env or Secret Manager)
# GEMINI_API_KEY=secret:projects/PROJECT/secrets/GEMINI_API_KEY
# TWITTER_LIST_URL=https://x.com/i/lists/...
# INTERACTIVE=0
# HEADLESS_BROWSER=true
# BROWSER_ARGS=--no-sandbox,--disable-dev-shm-usage
# PROFILE_DIR=/data/profile (optional if persistence mounted)

